# Codex開発ルール

このリポジトリは「疑問点管理アプリ」です。

変更を行う前に `README.md` と `SPEC.md` を確認してください。

機能追加・修正を行う場合は、以下のルールを守ってください。

## 1. 最優先事項

このアプリで最も重要なのは、

「疑問が浮かんだとき、できるだけ少ない操作ですぐ記録できること」

です。

新機能によってクイック登録が複雑にならないようにしてください。

疑問カードの新規登録で必須となるユーザー入力は「タイトル」のみです。

---

## 2. データを失わせない

既存ユーザーのカードや設定を、不必要に削除・破壊しないでください。

データ構造を変更する場合は、既存データへの影響を確認してください。

カードの通常削除では即時完全削除を行わず、まずゴミ箱へ移動してください。

---

## 3. ユーザーデータを分離する

すべてのユーザーデータはアカウントごとに分離してください。

あるユーザーが別のユーザーの、

- カード
- 領域
- 分野
- タグ
- 関連情報

を閲覧・変更できないようにしてください。

画面側だけでなく、Supabase側でもアクセスを制限してください。

---

## 4. 秘密情報をGitHubへ保存しない

以下のような秘密情報をリポジトリへ直接保存しないでください。

- パスワード
- Supabaseの秘密鍵
- APIの秘密鍵
- 個人情報
- その他の非公開認証情報

秘密情報が必要な場合は環境変数など適切な方法を使用してください。

---

## 5. カード状態

カードの状態は基本的に、

- 疑問
- 解決済み

の2種類です。

解決済みにするとき、カードを別のテーブルへコピー・移動しないでください。

同じカードの状態を変更してください。

解決済みカードを疑問へ戻せるようにしてください。

---

## 6. 未整理

重要度が設定されていない疑問カードを「未整理」として表示します。

「未整理」という別種類のカードを作らないでください。

重要度を設定すると、通常の重要度グループへ表示されるようにしてください。

---

## 7. 関連カード

カード同士の関連には方向を持たせません。

AとBの関係は、

A → B

ではなく、

A — B

です。

同じ関係を逆方向に重複保存しないでください。

---

## 8. PC・スマートフォン

機能追加・UI変更ではPCだけでなくスマートフォン表示も確認してください。

スマートフォンから疑問を素早く登録できることを特に重視してください。

---

## 9. 機能を過度に複雑にしない

初期バージョンでは、SPEC.mdに記載されていない大規模な機能を勝手に追加しないでください。

特に、

- AI機能
- SNS機能
- 共有機能
- 複雑な分類階層

などを、明示的な指示なしに追加しないでください。

---

## 10. コード構造

1つの巨大なファイルにすべての処理をまとめないでください。

機能ごとに責任を分け、後から一部分だけ変更しやすい構造にしてください。

特に、

- データ保存
- カード
- 領域・分野
- タグ
- 関連カード
- 検索
- グラフ
- Markdown / TeX
- 認証

などは、可能な限り役割を分けてください。

---

## 11. 仕様変更

実装によって仕様が変わる場合は、必要に応じて `SPEC.md` も更新してください。

データ構造に重要な変更を加える場合は、その変更内容が後から分かるようにしてください。

---

## 12. 変更時の基本姿勢

変更を行う際は、

1. 既存仕様を確認する
2. 関係するコードを確認する
3. 必要最小限の変更を行う
4. 既存機能を壊していないか確認する
5. PC・スマートフォンへの影響を確認する
6. 必要ならドキュメントを更新する

という順序を基本としてください。

ユーザーから小さな改善を依頼された場合、不必要にアプリ全体を書き換えないでください。

## 13. Step 6の実装と確認方法

- React / TypeScript / Viteを使用。認証は `src/auth/`、保存処理は `src/cards/api.ts`、接続設定は `src/lib/supabase.ts`。
- `pnpm install --frozen-lockfile` で依存関係を復元し、`pnpm build` で型とビルドを確認する。
- 認証・保存・一覧に変更があれば `pnpm test` を実行する。初回のみ `pnpm exec playwright install chromium` が必要。
- ブラウザーテストは架空の接続先と模擬通信を使う。実際のRLSが正しいという証明にはならない。
- `.env.local` をコミットしない。ブラウザー向けの `VITE_` 変数に秘密鍵を置かない。
- 既存Supabaseプロジェクトを別アプリと共有している。qm_以外のテーブル、共通の認証設定、Site URLを不用意に変更しない。
- データベース変更が必要な場合はSQLを履歴に残し、対象と既存データへの影響を説明する。Step 6には変更用SQLはなく、`sql/check-step5.sql` は読み取り専用。
- 動作範囲と未実装機能は `docs/STEP6.md` と `SPEC.md` の実装状況に記録する。
