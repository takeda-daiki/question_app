疑問点管理アプリ v1.0 仕様書

実装状況：メール・パスワード認証、タイトルのみの登録、カード詳細・編集、本文・結論、状態切替、重要度・労力、領域・分野とタグの管理、複合検索、方向なしの関連付け、領域別グラフ、Markdown・TeXの即時プレビュー、ゴミ箱・復元・完全削除、JSONエクスポート、スマートフォン下部ナビゲーションとホーム画面追加用manifestを実装。詳細・制限はdocs/FEATURES.mdを参照。

一覧は重要度（3→2→1→未整理）、同じ重要度では労力（低→中→高→未設定）、次に作成日時の新しい順。未整理は労力にかかわらず作成日時の新しい順。全データをページ分割で取得して検索・並べ替えを行い、一覧は50件ずつ描画する。大規模データ向けのサーバー側検索は今後の改善対象。

分類削除の仕様補足：カード（ゴミ箱を含む）や分野から参照されている領域・分野は削除を止め、先に分類変更を案内する。タグ削除はカード本体を残しタグ付けを解除する。グラフの手動配置は保存しない。JSONはエクスポートのみで復元インポートは未実装。オフライン編集は未対応。

実際の保存先は既存Supabaseプロジェクト内のqm_テーブル群（qm_cardsなど）。認証はSupabase Authを使用し、同一プロジェクトのユーザーは既存アプリと共通。Step 6はデータベース構造や共通の認証設定を変更しない。

1. 目的

疑問をすぐに記録し、後から整理・調査・関連付けを行い、最終的な結論を知識として蓄積する。

最も重視するのは「疑問を忘れる前に記録できること」。

2. 基本構造

情報は以下の構造で整理する。

領域
└ 分野
　└ 疑問カード

分類できないカードは「未分類」として扱う。

初期バージョンでは領域・分野の2段階までとし、それ以上の階層化は行わない。

3. 疑問カード

カード作成時の必須入力は「疑問タイトル」のみ。

任意情報

領域

分野

重要度

労力

本文

結論

タグ

関連カード

自動保存情報

カードID

ユーザー

状態

作成日時

更新日時

解決日時

削除日時

4. クイック登録

PC・スマートフォンのどちらでも、どの主要画面からでもすぐに疑問を追加できるようにする。

クイック登録の必須入力は「疑問タイトル」のみ。

追加画面の「領域・分野・タグを追加（任意）」から既存項目を選択し、各項目の「＋ 新規作成」からその場で作成・選択できる。詳細編集画面にも同じ導線を設ける。分野は選択中の領域に属する。同名項目は既存を再利用する。

分類・タグの新規項目は作成操作時に保存する。追加画面ではカードへの分類・タグ付けをカード保存時に確定する。詳細画面のタグ付けは従来どおり即時保存する。設定画面は名前・色・順序・削除などの管理用として残す。

その他の情報は入力しなくても保存できる。

詳細をその場で設定したい場合のみ「詳細を追加」から入力する。

領域・分野の画面から登録した場合は、その領域・分野を初期値として使用してよい。

それ以外の場合は未分類として登録する。

5. 未整理

重要度が設定されていない疑問を「未整理」として扱う。

未整理カードは疑問一覧の下部にまとめて表示する。

未整理内では新しいカードを上に表示する。

重要度を設定すると、自動的に適切な重要度グループへ表示が移る。

6. 重要度

重要度は以下の3段階。

★★★

★★

★

未設定も許可する。

通常の疑問一覧では、

★★★
★★
★
未整理

の順に表示する。

7. 労力

疑問を解決するために必要と予想される労力を、

低

中

高

で設定する。

未設定も許可する。

同じ重要度内では、

低 → 中 → 高

を基本的な表示順とする。

それも同じ場合は、新しく作成されたカードを上に表示する。

8. 状態

カードには、

疑問

解決済み

の2状態を持たせる。

画面上部には、

疑問

解決済み

すべて

の表示切り替えを用意する。

初期表示は「疑問」。

9. 解決

カードを「解決済み」に変更できる。

解決時には解決日時を保存する。

結論を書いてから解決することを基本とするが、結論が空でも解決済みにすることは許可する。

解決後も結論を編集できる。

解決済みカードを再び「疑問」に戻すこともできる。

カードを解決済みにしても別のデータへ移動させない。

同じカードの状態だけを変更する。

10. 領域

疑問一覧の領域・分野・タグの選択欄直下から新規作成できる。作成後はその項目を絞り込み条件として選択する。分野の追加には親の領域の選択が必要。カード自体の分類は変更しない。

ユーザーは領域を自由に、

作成

編集

削除

並び替え

できる。

領域には色を設定できるようにする。

11. 分野

領域の中に分野を作成できる。

ユーザーは分野を、

作成

編集

削除

並び替え

できる。

分野未設定のカードも許可する。

12. タグ

カードには複数のタグを設定できる。

タグは領域をまたいで使用できる。

タグを選択すると、そのタグが設定されているカードを確認できる。

13. 関連カード

カード同士を関連付けることができる。

1枚のカードに複数の関連カードを設定できる。

関連カードには方向を持たせない。

AとBを関連付けた場合、

A — B

という1つの関係として扱う。

AからB、BからAのどちらからでも関連カードとして確認できる。

カード詳細から関連カードを選択すると、そのカードへ移動できる。

14. 検索

以下を検索対象とする。

タイトル

本文

結論

タグ

領域

分野

検索は領域をまたいで行える。

以下による絞り込みを可能にする。

状態

領域

分野

重要度

労力

タグ

15. グラフ

疑問カード同士の関連をグラフとして表示する。

全領域をまとめた巨大なグラフは初期バージョンでは必要としない。

領域を選択して、その領域のカードを表示する。

表示対象は、

すべて

疑問のみ

解決済みのみ

から選択できる。

カードをノード、関連カードの関係を線として表示する。

最低限、

拡大・縮小

ノード移動

カードタイトル表示

ノード選択からカード詳細への移動

を可能にする。

16. Markdown

本文と結論ではMarkdownを利用できるようにする。

最低限、

見出し

太字

リスト

インラインコード

コードブロック

に対応する。

RやPythonなどのコードも記録できるようにする。

17. 数式

TeX形式による数式入力に対応する。

例：

$p(\theta|y)$

および

$$
p(\theta|y) \propto p(y|\theta)p(\theta)
$$

の形式を使用できる。

Obsidianに近く、保存しなくても入力中に数式の表示を確認できることを目標とする。

18. 削除

カードを削除しても即座に完全削除しない。

まずゴミ箱へ移動する。

ゴミ箱から、

復元

完全削除

を選択できる。

19. データ保存

クラウド上のデータ保存にはSupabaseを利用する。

主なデータは以下に分けて管理する。

users

areas

fields

cards

tags

card_tags

card_links

カード同士の関連は方向なしの関係として保存する。

20. アカウント

ユーザーごとにデータを完全に分離する。

他のユーザーのカード・領域・タグなどを閲覧・変更できないようにする。

初期バージョンではメールアドレスとパスワードによるログインに対応する。

将来的にGoogleやAppleなどによるログインを追加できる構造にする。

21. PC・スマートフォン

PCとスマートフォンの両方から同じデータを利用できるようにする。

スマートフォンではホーム画面へ追加してアプリに近い形で利用できるようにする。

PC

基本的に、

左：領域

中央：疑問一覧

を中心とするシンプルなカード一覧型とする。

スマートフォン

1画面ずつ表示する。

下部ナビゲーションを基本とし、

疑問

検索

＋

グラフ

設定

へアクセスできるようにする。

22. 主要画面

初期バージョンでは以下を中心とする。

疑問一覧

クイック登録

カード詳細

検索

グラフ

設定

ゴミ箱

画面構成は機能性を優先し、初期段階ではデザインを過度に作り込まない。

23. バックアップ

ユーザーのデータをバックアップできるようにする。

初期バージョンではJSON形式でのエクスポートを基本とする。

24. 保存状態

ユーザーがデータの保存状況を確認できるよう、

保存中

保存済み

保存失敗

などの状態を必要に応じて表示する。

25. 初期バージョンで実装しないもの

以下は将来機能とする。

AIによる自動回答

AIによる自動タグ付け

AIによる関連カード提案

SNS機能

他ユーザーとの共有

公開カード

全領域をまとめた巨大グラフ

3段階以上の分類階層

Obsidianとの直接同期

26. 将来的な拡張候補

AIによる疑問整理

AIによる関連カード提案

AIとのQ&A

長期間未解決の疑問の通知

学習状況の統計

App Store / Google Playでの配布

27. 優先順位

最も重要な順に、

疑問を忘れる前に最小限の操作で記録できる

後から簡単に整理できる

後から簡単に探せる

重要度・労力によって取り組む疑問を判断できる

解決した内容を知識として残せる

疑問同士を関連付けられる

PC・スマートフォンの両方で快適に利用できる

データを安全に保存できる

後から機能を変更・追加しやすい

ことを重視する。
